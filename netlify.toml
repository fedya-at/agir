[build]
  base = "client/app"
  publish = "dist"
  command = "npm run build"

# Handle React Router Single Page Application routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
